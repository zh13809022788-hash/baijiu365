export const SITE_URL = 'https://baijiu365.com';
export const SITE_NAME = 'Baijiu365';

// AdSense - keep empty before approval; real ads render only when both values exist.
export const ADSENSE_CLIENT_ID = '';
export const ADSENSE_SLOTS = {
  banner: '',
  'in-article': '',
  sidebar: '',
} as const;

// Affiliate links - keep empty before approval; CTAs render only for real http(s) URLs.
export const AFFILIATE_LINKS = {
  // Example: baijiuBuy: '', maotaiStore: ''
} as const;
